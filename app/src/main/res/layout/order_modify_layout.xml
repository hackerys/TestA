<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/confirm_modify"
        android:layout_width="match_parent"
        android:layout_height="96px"
        android:layout_alignParentBottom="true"
        android:background="@drawable/btn_selector_confirm"
        android:gravity="center"
        >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="确认修改"
            android:textColor="@color/white"
            android:textSize="@dimen/common_text_size0"
            />
    </LinearLayout>

    <ScrollView
        android:id="@+id/good_list_home"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/confirm_modify"
        >

        <com.example.administrator.testa.view.NoScrollView
            android:id="@+id/good_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:descendantFocusability="beforeDescendants"
            android:listSelector="@color/white"></com.example.administrator.testa.view.NoScrollView>
    </ScrollView>
</RelativeLayout>